server:
  port: 8080
  shutdown: GRACEFUL
  compression.enabled: false

spring:
  application.name: distributedcounter
  cloud.kubernetes.enabled: false  # Enabled on kubernetes profile
  main:
    lazy-initialization: true
    web-application-type: REACTIVE
