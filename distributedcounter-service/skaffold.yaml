apiVersion: skaffold/v2beta7
kind: Config
metadata:
  name: distributedcounter-service
build:
  artifacts:
  - image: bkoprucu/distributedcounter
    jib: {}
#  spring boot provides a build-image plugin since 2.3 which can replace jib, but it is slower
#    custom:
#      buildCommand: mvn -DskipTests -Dspring-boot.build-image.imageName=$IMAGE spring-boot:build-image
#      dependencies:
#        paths:
#          - src
#          - pom.xml
  tagPolicy:
    dateTime: {}

deploy:
  kubectl:
    manifests:
    - k8s/configmap.yml
    - k8s/deployment.yml
    - k8s/rbac.yml
