# Distributed counter

An example REST microservice which counts events incrementally. 

## Prerequisites

  * Maven 3
  * Java JDK 8
  
  
## Quick start

  * Externalized configuration has not been implemented. So is service discovery. 
    <br>To configure ports and hosts edit following file:

    ```
    service/src/main/java/org/bashar/distributedcounter/Preferences.java
    ```
    You may skip this if you want to test it on your host using default port 8080 for http, 9500 for Hazelcast.
  * Build using `maven install`

  * You may distribute fat jar `service/target/service-1.0-SNAPSHOT.jar` to other nodes
  
  * Run the service
    ```
    $ java -jar service/target/service-1.0-SNAPSHOT.jar
    ```

## Test the service
  Add parameter `-v` to following commands to see more details of the response.
  <br>
  #### Increment the count of an event
  Event identified with "Event1"
  ```
  $ curl -w "\n" -X PUT http://localhost:8080/counter/increment -H "Content-Type: application/json" -d '{"id":"Event1"}'
  $ _
  ```
  #### Get the count of an event
  ```
  $ curl -w "\n" http://localhost:8080/counter/count?event_id=Event1
  {"id":"Event1","count":6}
  $ _
  ```
  #### List all counters
  ```
  $ curl -w "\n" http://localhost:8080/counter/counters
  [{"id":"Event4","count":1},{"id":"Test Id","count":1},{"id":"Event3","count":1},{"id":"Event1","count":6}]
  $ _
  ```

## Using the client
  
  **Client implemented with Apache Http Client is provided. Your project will need following jar generated by build:**
  <br><br>
  `client/target/client-1.0-SNAPSHOT.jar`
  <br><br>
  Or add the dependency:
  <br>
  ```
  <dependency>
      <groupId>org.bashar.distributedcounter</groupId>
      <artifactId>client</artifactId>
      <version>1.0-SNAPSHOT</version>
  </dependency>
  ``` 
  <br>
  
  **Sample usage of the client and some performance tests are in:**
  <br><br>
  `integrationtest/src/test/java/org/bashar/distributedcounter/client/DistributedCounterClientTest.java`
  

## Author

* **Başar Köprücü** [bkoprucu](https://github.com/bkoprucu) - [linkedIn](https://www.linkedin.com/in/koprucu/)

## License

This project is licensed under the MIT License - see the [LICENSE.md](LICENSE.md) file for details
